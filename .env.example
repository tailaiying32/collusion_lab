# Required for OpenAI backend
OPENAI_API_KEY=

# Optional: only required when using Anthropic backend
ANTHROPIC_API_KEY=

# Optional: only required when using DeepSeek backend
DEEPSEEK_API_KEY=

# Optional: only required when using SQLite/Postgres/Neon for external storage
COLLUSIONLAB_STORAGE_URL=
